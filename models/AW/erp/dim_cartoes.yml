version: 2

models:
  - name: dim_cartoes
    description:
      Tabela dimensão com informações de cartões da AdventureWorks.
    columns:
      - name: FK_CARTAO
        description: Chave primária da dimensão de cartões.
        tests:
          - unique
          - not_null